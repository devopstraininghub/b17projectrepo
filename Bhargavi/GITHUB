GITHUB:

created the github account
created the project repo in central repository
git clone <central project repo link>
ll --> b17projectrepo
cd b17projectrepo
touch fname
git status --> red
git add . 
git status --> green
git commit -m " commit message"
git status
git log
git push --> fname pushed to central repo
if we get an error 
git pull --rebase
git push
git show <commit id> : to view the changes done in a specific commit id

Linix Editor - vi/vim/nano --> to add content into the files.
vim <file_name>
cat : to view content in the file
cat <file_name>
